apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}-service-lb
  labels:
    app: "{{ .Values.name }}"
spec:
  type: LoadBalancer
  selector:
    app: "{{ .Values.name }}"
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000

